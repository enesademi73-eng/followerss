# Follower App

## Prerequisites

- Node.js and npm installed
- Python and Flask installed

## Installation

1. Clone the repository
2. Navigate to the project directory
3. Run the setup script: `bash scripts/setup.sh`

## Running the Application

1. The setup script will start the frontend and backend servers.
2. Access the application at `http://localhost:3000/`

## Logging

The backend server logs each login attempt to `app.log`.

## Setup Script

1. Navigate to the project directory.
2. Run the setup script: `bash scripts/setup.sh`
